server:
  port: 8080
  servlet:
    context-path: /shopping

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/backend_shop"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true